import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-vue-modal-event-bug',
  webDir: 'dist'
};

export default config;
